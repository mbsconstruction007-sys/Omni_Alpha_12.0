name: Basic Check

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Basic Check
        run: |
          echo "Omni Alpha 12.0 - Build Successful"
          ls -la
